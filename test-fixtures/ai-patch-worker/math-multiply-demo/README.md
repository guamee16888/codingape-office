# Math Multiply Demo

Small math fixture for evaluating a focused `multiply` addition.

The existing `add` and `divide` behavior is already healthy. The intended task is to add and verify `multiply(a, b)`.
